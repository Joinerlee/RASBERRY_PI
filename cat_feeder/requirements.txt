# 기본 하드웨어 제어 및 이미지 처리
RPi.GPIO>=0.7.0          # 라즈베리파이 GPIO 핀 제어를 위한 라이브러리
picamera2>=0.3.9         # 라즈베리파이 카메라 모듈 제어
numpy>=1.21.0            # 수치 계산 및 배열 처리
Pillow>=8.3.1            # 이미지 처리 및 변환

# 웹 서버 및 API 관련
fastapi>=0.68.0          # restapi 통신용용
uvicorn>=0.15.0          # ASGI 서버 - FastAPI 구동용
python-multipart>=0.0.5  # 파일 업로드 처리

# 클라우드 스토리지
firebase-admin>=5.0.0    # Firebase 서비스 연동 (이미지 저장 및 데이터베이스)

# 유틸리티 패키지
python-dotenv>=0.19.0    # 환경 변수 관리 (.env 파일 사용)
pydantic>=1.8.2          # 데이터 검증 및 설정 관리
schedule>=1.1.0          # 작업 스케줄링 (정해진 시간에 사료 급여)
loguru>=0.5.3            # 로깅 라이브러리 (에러 및 디버그 로그 관리)

# 테스트 관련
pytest>=6.2.5            # 파이썬 테스트 프레임워크
pytest-asyncio>=0.15.1   # 비동기 코드 테스트 지원